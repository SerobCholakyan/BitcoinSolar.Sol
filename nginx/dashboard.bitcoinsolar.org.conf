server {
    listen 80;
    server_name dashboard.bitcoinsolar.org;

    root /var/www/dashboard.bitcoinsolar.org;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }
}
